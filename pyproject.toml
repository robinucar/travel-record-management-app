[project]
name = "travel-record-management-app"
version = "0.1.0"
description = "A GUI based record management system for a specialist travel agent."
requires-python = ">=3.14"

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B"]
